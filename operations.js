export const sum = (a, b) => a + b;
export const minus = (a, b) => a - b;

export const multiply = (a, b) => a * b;
export const devision = (a, b) => a / b;

export const firstName = "Yacine";

const msg = () => {alert("hello world !")};

export default msg;